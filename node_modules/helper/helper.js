var util       = require('util');
;var time = {};

exports.showAgent = function (req) {
	util.log("Host= " + (req.headers["x-real-ip"] || req.connection.remoteAddress) +  "\n Url=" + req.url + "\n User-agent=" +req.headers["user-agent"]);
};


exports.time = {
	start: function() {
			time.value = Date.now();
	},
	stop: function() {
			return Date.now() - time.value;
	},
	
};